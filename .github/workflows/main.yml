name: TEST

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      SHORT_SHA: ${GITHUB_SHA::7}
    steps:
      - name: Checkout
      - uses: actions/checkout@v3

      - name: Echo
        run: echo $SHORT_SHA
